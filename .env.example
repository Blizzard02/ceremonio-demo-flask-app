# Copy to your environment / PythonAnywhere WSGI config. Do NOT commit real secrets.
# Local dev defaults to sqlite if DATABASE_URL is unset.
DATABASE_URL=mysql+pymysql://<user>:<password>@<user>.mysql.pythonanywhere-services.com/<user>$glenti
SECRET_KEY=replace-with-a-long-random-value
